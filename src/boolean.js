const isDrawing = false;
const age = 18;
const ofAge = age > 19;
console.log(ofAge);

//-------------------------
      `=` sign : used for assignment/updation of value 
      `===` checks both type & value. 
      console.log(age===18);

      `==` only checks value. 
      const s1 = '18';
      console.log(s1==18);

        10 === "10" //false as value are same but types are not
        10 == "10" //true as value are same